import { QuotesHighlightDirective } from './quotes-highlight.directive';

describe('QuotesHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new QuotesHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
